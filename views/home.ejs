<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <link
      href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body
    class="min-w-screen text-white flex justify-center bg-black"
  >
  <div class="h-screen w-1/6 flex align-center m-2 p-1">
    <div
      class="h-3/6 p-2 bg-gray-800 w-full self-center flex flex-col justify-center align-center"
    >
      <h1 class="text-3xl self-center font-bold font-weight-800 m-2">
        <%= username %>
      </h1>
      <div class="w-2/3 h-1/3 bg-gray-200 self-center rounded-xl">
        <!-- <img src="" alt=""> -->
      </div>
      <p class="text-2xl font-semibold self-center m-2"><%= email %></p>
      <div class="flex flex-col align-center items-center justify-center">
        <form action="/edit-username" method="post">
          <input type="hidden" name="username" value="<%= username %>">
          <button
            class="self-start px-3 py-2 w-full m-1 text-white bg-gray-700 rounded-md" type="submit"
          >
            edit username
          </button>
        </form>
        <form action="/sign-out">
          <button
            class="self-start px-3 py-2 w-full m-1 text-white bg-gray-700 rounded-md"
          >
            upload new profile pic
          </button>
        </form>
        <form action="/log-out">
          <button
            class="self-start px-3 py-2 w-full m-1 text-white bg-red-700 rounded-md"
          >
            Log-out
          </button>
        </form>
      </div>
    </div>
  </div>


    <div class="w-1/5 p-2 bg-gray-800">
      <div class="p-2 w-full">
        <form
          action="/newpost" method="post"
          class="flex flex-col w-full justify-center align-center items-center"
        >
          <p class="text-xl text-gray-300 self-start font-semibold">
            Add new post
          </p>

          <div class="h-px w-full bg-gray-300 m-2"></div>
          <textarea
            placeholder="Write your post..."
            type="textarea" name="content" required
            class="w-full bg-gray-900 p-1 ps-2 h-28 outline-none border-gray-300 resize-none border-1"
          ></textarea>
          <button
            type="submit"
            class="px-3 text-md m-2 py-2 bg-blue-600 text-white self-center rounded-md"
          >
            Post
          </button>
        </form>

        
      <!-- post area here onwards -->
      <div class="h-px w-full bg-gray-300 m-1"></div>
        <% if( allPostContent && allPostContent.length>0){ %>
        <% allPostContent.reverse().forEach((postcontent) => {%>
          <div class="w-full self-center flex flex-col bg-gray-700 p-2 m-2 rounded-md align-center">
            <p class="self-start text-md text-gray-300"><%= username %></p>
            <p class="self-start text-sm text-gray-300"><%= email %></p>
            <h3 class="self-center text-white bg-gray-800 w-full text-md m-1 break-words font-semibold"><%= postcontent %></h3> 
            <div class="w-full flex justify-between">
            </div>
          </div>
      <% }); } else{%> 
        <p class="text-2xl font-semibold text-gray-300">No post yet</p> 
      <%}%>


      </div>





    </div>
    <!-- <div class="h- min-h-[10%] bg-gray-200">f</div> -->
  </body>
</html>
